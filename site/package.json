{
  "name": "kaden-mission",
  "version": "1.0.0",
  "description": "Kaden's Mission Homecoming - Interactive timeline website",
  "scripts": {
    "dev": "eleventy --serve --port=8181",
    "build": "eleventy",
    "clean": "rm -rf _site",
    "deploy": "./deploy.sh"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
